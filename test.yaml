paths:
  /xss:
    get:
      summary: "<img src=x onerror=alert('xss')>"
      parameters:
        - name: param
          in: query
          description: "<script>alert(1)</script>"
          required: false
          schema:
            type: string
            